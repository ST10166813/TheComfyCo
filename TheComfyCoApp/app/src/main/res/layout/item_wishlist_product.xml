<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardUseCompatPadding="true"
    app:cardCornerRadius="20dp"
    app:cardElevation="6dp"
    app:strokeWidth="0dp"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="16dp">

        <!-- Product Image -->
        <ImageView
            android:id="@+id/imgWishlistItem"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:scaleType="centerCrop"
            android:layout_marginEnd="16dp" />

        <!-- Product info -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvWishlistItemName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end"
                android:fontFamily="@font/tenor_sans_reg"
                android:textColor="?attr/colorOnSurface" />

            <TextView
                android:id="@+id/tvWishlistItemPrice"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                android:fontFamily="@font/tenor_sans_reg"
                android:textColor="?attr/colorOnSurface"
                android:layout_marginTop="4dp" />

            <!-- NEW: Description -->
            <TextView
                android:id="@+id/tvWishlistItemDescription"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="13sp"
                android:maxLines="2"
                android:ellipsize="end"
                android:layout_marginTop="6dp"
                android:fontFamily="@font/tenor_sans_reg"
                android:textColor="?attr/colorOnSurface" />

        </LinearLayout>

        <!-- Trash icon -->
        <ImageView
            android:id="@+id/btnWishlistDelete"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/icon_remove"
            android:padding="4dp"
            android:contentDescription="@string/btn_remove_from_wishlist" />
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
